<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.weboard.mapper.UserMapper">
    <select id="getUserById" parameterType="Integer" resultType="com.example.weboard.dto.UserDTO">
        SELECT * FROM user where id = #{id}
    </select>

    <select id="getIdByUserId" parameterType="String" resultType="Integer">
        SELECT id FROM user WHERE user_id =#{userId}
    </select>

    <select id="getPasswordById" parameterType="int" resultType="String">
        SELECT password FROM user WHERE id = #{id}
    </select>

    <insert id="insertUser" parameterType="com.example.weboard.dto.UserDTO">
        INSERT INTO user (user_id, nickname, password, updated_by, updated_at)
        VALUES (#{userId}, #{nickname}, #{password}, null, null)
    </insert>

    <update id="updateUser" parameterType="com.example.weboard.dto.UserDTO">
        UPDATE user SET user_id = #{userId},
                        nickname = #{nickname},
                        password = #{password},
                        updated_by = #{userId},
                        updated_at = #{updatedAt}
            WHERE id=#{id}
    </update>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM user
            WHERE id = #{id}
    </delete>

</mapper>