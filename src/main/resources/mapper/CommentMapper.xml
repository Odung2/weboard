<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.weboard.mapper.CommentMapper">

    <select id="getCommentByPostId" parameterType="int" resultType="com.example.weboard.model.Comment">
        SELECT * FROM comment WHERE post_id=#{postId}
    </select>

    <insert id="insertComment" parameterType="com.example.weboard.model.Comment">
        INSERT INTO comment (post_id, user_id, comment_text, created_by, updated_by, updated_at)
        VALUES ( #{postId}, #{userId}, #{commentText}, #{userId}, null, null)
    </insert>

    <update id="updateComment" parameterType="com.example.weboard.model.Comment">
        UPDATE comment SET comment_text=#{commentText},
                        updated_by=#{updatedBy},
                        updated_at=#{updatedAt}
            WHERE comment_id=#{commentId}
    </update>

    <delete id="deleteComment" parameterType="int">
        DELETE FROM comment
            WHERE comment_id=#{commentId}
    </delete>

</mapper>